# 휴가(Leave) Frontend 개발 ROADMAP

> 출처 PRD: `docs/prd/6.leave-prd.md` (groupware-frontend-prd-generator 생성 · groupware-prd-validator 검증 통과 — Blocking 결함 2건 해결: 요약 응답 `empId` 백엔드 보강[#5] / 결재대기함 포함으로 결재자 상세 진입 확보[#8])
> 이 로드맵은 PRD를 **실행 순서·의존성·태스크**로 전개한 것이다. 기능/계약의 사실 원천은 PRD와 백엔드 계약 문서(`api-endpoint.md` DRAFT/DOCUMENT API + EMP LEAVE/DEPT API + `back/build/generated-snippets/<기능ID>/`)이며, 여기서는 **무엇을 어떤 순서로 만들지**만 다룬다. 필드/DTO는 재서술하지 않고 PRD §참조 계약 매핑을 가리킨다.

## 🗺️ 개요

**📅 최종 업데이트**: 2026-07-08
**📊 진행 상황**: 0/39 Tasks 완료 (0%) — M1~M7 전부 대기 (실행 리프 태스크 39개, split 부모 T3.4·T6.4 제외 집계)

- **전략**: walking-skeleton-first 세로 슬라이스. 단, **아키텍처 배관(M0)·인증·셸·근태 도메인은 이미 완료**되어 있으므로 재구축하지 않고 **소비**한다. 휴가 도메인은 조회 슬라이스(내 휴가) → 상세 read-only → 작성/수정 → 상세 액션(기안자/결재자) → 부서/관리자 조회 순으로 얇게 관통한다.
- **소비할 기존 배관(재구축 금지)**:
  - axios 단일 인스턴스·401/reissue 인터셉터·`withCredentials`: `src/shared/api/client.ts`
  - QueryClient·retry/staleTime 방침: `src/shared/api/queryClient.ts`
  - 에러 정규화·에러코드→UI 매핑 헬퍼: `src/shared/lib/apiError.ts` (`ROLE_003` 권한부족 UX 포함)
  - 보호 라우트·role 가드: `src/shared/components/ProtectedRoute.tsx`, `src/shared/lib/hasRequiredRole.ts`
  - 레이아웃 셸·사이드바 메뉴 슬롯: `src/shared/components/LayoutShell.tsx`, `sidebarMenuItems.ts`("휴가 관리" 그룹 placeholder 2개 이미 선언됨: `내 휴가 요약`/`부서 휴가 관리`)
  - 본인 부서(`deptId`) 도출 훅: `src/features/attendance/model/usePrimaryDeptId.ts` — 이미 구현됨. **재사용**(필요 시 `src/shared`로 승격 검토, 신규 작성 금지)
  - 사원 프로필/`empId`(본인) 출처: `src/features/employee/api/useMeQuery.ts`
  - 페이징 표·필터·react-table 목록 패턴: `src/features/board/**`, `src/features/attendance/**` 복제 대상
  - queryKey 팩토리 컨벤션: `src/features/attendance/model/queryKeys.ts` / `src/features/board/**` 동형 복제
  - 날짜/일시 생성·`yyyy-MM-dd'T'HH:mm:ss` 포맷: `dayjs` / 토스트: `sonner` / 폼: `react-hook-form + zod` (CLAUDE.md §6 고정 스택)
- **범위 경계**: PRD §"MVP 이후 기능(이번 PRD 명시적 제외)"은 로드맵 범위 밖(§백로그 참조, 태스크화 금지). 특히 공용 문서함 3종(결재함/기안함/임시저장함)·공람·일반/출장/매출 기안 작성·첨부 업로드/뷰어는 전자결재/파일 도메인 관할이며, **결재대기함(F619)만 결재자 상세 진입 확보를 위해 최소 포함**한다.
- **완료 게이트(전 마일스톤 공통)**: `npm run check-all` 통과 + `npm run build` 성공(CLAUDE.md §9). 도메인 슬라이스 구현 직후 test-author-runner로 유닛/컴포넌트 테스트 작성·실행.

## 🧩 의존성 개요

```
[이미 완료된 배관: M0 배관 · 인증 슬라이스 · 셸/사원뷰 · 근태 M1~M4]  ← 소비만 함(재구축 금지)
  └→ M1 내 휴가 조회 슬라이스 (F604 이력 + F605 잔여)               ← 휴가 도메인 스캐폴딩(model/queryKeys/api/label·badge 유틸) 최초 생성
        ├→ M2 휴가 기안서 상세 조회 슬라이스 (F606 read-only)          ← 내 휴가 이력행→상세 관통. DraftDetail 타입·상세 페이지 셸 = 모든 상세 액션의 대지(canvas)
        │     ├→ M3 휴가 기안서 작성/수정 슬라이스 (F601·F602·F603)      ← 작성→상세 landing, 수정=상세(F606) 프리필
        │     ├→ M4 기안자 상세 액션 슬라이스 (F607·F608·F609)           ┐ 둘 다 M2 상세 페이지 셸에만 의존
        │     └→ M5 결재대기함+결재자 액션 슬라이스 (F619 · F610·F611)    ┘ → 상호 독립·병렬 착수 가능
        ├→ M6 부서 휴가 관리 조회 슬라이스 (F612·F613·F614)             ┐ 둘 다 M1에만 의존(조회·요약 부분)
        └→ M7 관리자 휴가 현황 슬라이스 (F615·F616 조회 + F617·F618 조정) ┘ → M2~M5 클러스터와도 병렬(이력행→상세 배선만 M2 대기)
```

- **M4와 M5는 둘 다 M2에만 의존** → 병렬 착수 가능(기안자 액션 슬라이스와 결재자 액션 슬라이스를 동시에).
- **M6·M7의 조회·요약 부분은 M1에만 의존** → M2~M5 클러스터와 병렬 착수 가능. 단 "이력 행 클릭 → 상세" 네비게이션 배선은 M2(상세 페이지) 완료를 기다린다(§병렬화 지점 참조).

## 🚩 마일스톤 & 태스크

### M1 — 내 휴가 조회 슬라이스 (읽기 우선 세로 슬라이스 + 도메인 스캐폴딩)

> 목표: 사이드바 "휴가 관리 > 내 휴가" 진입 → 내 잔여 휴가 카드 + 신청 이력 표가 실제로 그려지는 얇은 슬라이스. 근거: PRD §사용자 여정(내 휴가), §페이지별 상세(내 휴가 페이지), F604·F605.
> 완료 정의: `EMPLOYEE`가 메뉴로 진입해 연도 선택·상태/월 필터로 잔여 요약·신청 이력을 조회. **이력은 배열 응답(비페이징)**, 잔여는 부여−사용 프론트 계산.
> 이 마일스톤이 휴가 도메인 스캐폴딩(`features/leave/{model,api,components,pages,lib}`)을 최초로 만든다 — 이후 모든 슬라이스가 복제·소비.

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T1.1 | 휴가 도메인 타입 정의: `LeaveType` enum(ANNUAL/HOURLY/SICK/OFFICIAL/COMPENSATORY/SPECIAL)·`ApprovalStatus` enum·`MyLeaveHistory`·`EmpLeaveSummary`(필드 상세는 스니펫 실측, 여기서 재서술 안 함) | §참조 계약 매핑, `MY_LEAVE_REQUEST_HISTORY`/`MY_EMP_LEAVE_SUMMARY` | — | `features/leave/model/leave.ts`에 타입 존재, 스니펫과 대조 통과(contract-conformance) | 9 | 3 | ☐ |
| T1.2 | `leaveKeys` queryKey 팩토리(board/attendance 동형: `all`/`myHistory(params)`/`mySummary(year)` 선언, 이후 슬라이스가 축 확장) | §기술 스택, §참조 계약 매핑 | — (T1.1과 병렬) | `invalidateQueries(leaveKeys.all)`로 하위 일괄 갱신 가능한 구조 | 8 | 2 | ☐ |
| T1.3 | 표시 유틸(lib): `LeaveType` 라벨 매핑(HOURLY 라벨 Open Q#3)·`ApprovalStatus` 배지 매핑(표시명 문자열 ↔ enum 코드 필터)·`requestedLeaveDays`(1.0/0.5)·`reservedHours`("n시간") 표기(dayjs) | §참조 계약 매핑(enum·표시 규칙), Open Q#3 | T1.1 | 6개 LeaveType 라벨 + 5개 ApprovalStatus 배지 + 일수/시간 포맷 헬퍼 존재 | 7 | 3 | ☐ |
| T1.4 | API 함수 + query 훅: 내 휴가 이력(F604, `approvalStatus`/`yearMonth` optional, **배열 응답=비페이징**), 내 잔여 요약(F605, `year` optional) | F604·F605, §참조 계약 매핑 | T1.1, T1.2 | axios 인스턴스 소비, 이력은 배열(Page 아님) 그대로, 잔여는 단일 객체. 잔여=부여−사용 계산은 페이지 층에서 | 9 | 5 | ☐ |
| T1.5 | 내 휴가 페이지 조립: 연도 선택(yyyy, 기본=올해)·잔여 카드(연차/특별/포상 부여·사용·잔여 계산)·이력 표(react-table, `approvalStatus`/`yearMonth` 필터, **페이징 없음**)·[휴가 신청] 버튼 | §페이지별 상세(내 휴가), F604·F605 | T1.3, T1.4 | 카드/표/필터 렌더·상호작용, 조회 실패 시 에러 토스트(apiError 매핑 소비), [휴가 신청] 버튼 존재(라우팅은 T3.5) | 6 | 6 | ☐ |
| T1.6 | 라우트 승격: `/leave/me`(가칭) ProtectedRoute 자식 추가 + 사이드바 "내 휴가 요약" placeholder→`implemented:true`+`to` 승격 및 라벨 **"내 휴가"**로 조정 | §메뉴 구조, §사용자 여정 | T1.5 | 미인증→로그인 리디렉션(기존 가드), `EMPLOYEE` 메뉴 클릭→페이지 진입 | 4 | 3 | ☐ |

*(라우트 경로 최종 확정·router.tsx/sidebarMenuItems.ts 배선은 react-router-developer 위임 권장. `/leave/me` 경로명은 착수 시 확정 — §Open Questions 참조.)*

> **M1 split 판단(복잡도·중요도)**: 전 태스크 복잡도 < 7 → **split 없음(6개 전부 단일 task 유지)**. T1.1(연관 기능ID 2개·순수 타입선언, 로직 없음, 휴가 전 슬라이스 기반이라 최고 중요도 9)·T1.2(선언적 팩토리)·T1.3(단일 도메인 순수 표시 유틸, enum 3종 매핑)은 낮은 복잡도(2~3). T1.4(F604+F605 2개 엔드포인트지만 axios+react-query 표준 패턴 반복 — **단 F604는 Page가 아닌 배열 응답이라 페이징 미매핑 주의**)와 T1.5(연도·상태·월 필터 + 잔여 계산 카드 + 비페이징 표 조립, 단 라우팅/사이드바는 T1.6으로 분리)는 각각 5·6으로 7 미만 유지. T1.6은 기존 placeholder 승격 수준(신규 로직 없음)이라 복잡도 3.
> **실행 순서**: T1.1(중요도9) → T1.2(중요도8, T1.1과 병렬 가능) → T1.4(중요도9, T1.1·T1.2 의존) → T1.3(중요도7, T1.1 의존·T1.4와 병렬 가능) → T1.5(T1.3·T1.4 의존) → T1.6(T1.5 의존, M1 리프). 근거: 위상정렬(Depends-on) 우선 + 동순위 내 중요도 높은 순. T1.1·T1.2·T1.3·T1.4는 M2~M7 전 슬라이스가 재사용하는 기반이라 M1 내 최고 중요도.

### M2 — 휴가 기안서 상세 조회 슬라이스 (read-only, 상세 페이지 셸)

> 목표: 내 휴가 이력 행 클릭 → 휴가 기안서 상세를 읽기 전용으로 렌더하는 슬라이스(기안 정보·결재선 타임라인·첨부 목록 표시). 근거: PRD §사용자 여정(상세), §페이지별 상세(휴가 기안서 상세), F606.
> 완료 정의: `draftId`로 상세 조회 → drafter/title/content/`leave{startAt,endAt,leaveType,reservedHours}`·결재선(order/approvedAt/rejectedAt/rejectReason)·`files[]`(표시만)·상태 배지가 렌더. **액션 버튼은 다음 슬라이스(M3~M5)에서 배선** — 이 슬라이스는 read-only 대지(canvas)를 만든다.
> 이 상세 페이지 셸과 `DraftDetailResponse` 타입은 F607~F611이 전부 소비하므로 클러스터의 관문이다.

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T2.1 | 상세 타입 정의: `DraftDetailResponse`(draftType·drafter·approvers[]·files[]·`leave:LeaveDraftDetail\|null`·sourceDraftId/cancellationDraftId 등, 필드 상세는 `DRAFT_DETAIL` 스니펫/DTO 실측) | §참조 계약 매핑(`DRAFT_DETAIL`) | T1.1 | `features/leave/model/draftDetail.ts`에 타입 존재, 스니펫 대조 통과. F607~F611이 재사용할 기반 | 9 | 4 | ☐ |
| T2.2 | `leaveKeys`에 상세 축 추가(`draftDetail(draftId)`) + API 함수·query 훅: 상세 조회(F606) | F606, §참조 계약 매핑 | T2.1, T1.2 | 단건 조회 훅, 404/403(`ROLE_003`)→apiError 권한부족/not-found 매핑 소비 | 9 | 4 | ☐ |
| T2.3 | 상세 페이지 조립(read-only): 기안 정보 섹션 + 결재선 타임라인(approvers order/상태) + `leave` 상세(유형·기간·reservedHours) + 첨부 목록 표시(`files[]`, 업로드 없음 Open Q#6) + 상태 배지(T1.3 재사용) | §페이지별 상세(휴가 기안서 상세), F606 | T2.2, T1.3 | 상세 필드·결재선·첨부·배지 렌더. 액션 버튼 슬롯만 확보(비어 있음), 조회 실패 에러 토스트 | 7 | 6 | ☐ |
| T2.4 | 라우트 승격: `/leave/drafts/:draftId`(가칭) ProtectedRoute 자식 추가 + 내 휴가(T1.5) 이력 행 클릭→상세 네비게이션 배선 | §메뉴 구조(하위 라우트), §사용자 여정 | T2.3, T1.5 | 내 휴가 이력 행 클릭 → 상세 페이지 진입, 직접 URL 진입도 동작 | 5 | 3 | ☐ |

> **M2 split 판단(복잡도·중요도)**: 전 태스크 복잡도 < 7 → **split 없음(4개 전부 단일 task 유지)**. T2.1(순수 타입선언이나 `DraftDetailResponse`는 필드가 많고 F607~F611 전부의 기반이라 복잡도 4·중요도 9)·T2.2(단건 GET+403/404 매핑, 표준 패턴이나 상세 클러스터의 관문이라 중요도 9)는 M2 최고 중요도. T2.3(상세 필드가 많고 결재선 타임라인·첨부 목록까지 렌더하나 전부 presentational read-only라 6, 액션 배선은 M3~M5로 분리)은 임계값 미만. T2.4는 T1.6과 동형 라우트 승격 + 행 네비게이션이라 복잡도 3.
> **실행 순서**: T2.1(중요도9) → T2.2(중요도9, T2.1 의존) → T2.3(중요도7, T2.2·T1.3 의존) → T2.4(중요도5, T2.3·T1.5 의존, M2 리프). 근거: 위상정렬 우선 + 동순위 내 중요도 높은 순. T2.1/T2.2가 F607~F611(M3~M5)까지 재사용되는 기반이라 M2 최고 중요도.

### M3 — 휴가 기안서 작성/수정 슬라이스 (F601·F602·F603)

> 목표: 내 휴가 [휴가 신청] → 작성 폼 → [임시저장]/[상신] → 성공 시 상세(M2)로 이동. 상세 [수정] → 기존 값 프리필 → 부분 수정 저장. 근거: PRD §사용자 여정(작성), §페이지별 상세(휴가 기안서 작성), F601·F602·F603.
> 완료 정의: RHF+zod 폼(제목/본문/유형/시작·종료 일시/결재선)으로 임시저장(201)·상신(201) → 반환 `draftId`로 상세 이동 + 성공 토스트. 수정 모드는 F606 프리필 → F603 부분 수정(204) → 상세 재조회. 종료≥시작 refine, 상신 시 결재선 1명 이상(서버 판정).

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T3.1 | 작성/수정 폼 zod 스키마: `param.title`·`param.content` 필수, `leaveType` enum 필수, `startAt`·`endAt`(`yyyy-MM-dd'T'HH:mm:ss`) 필수 + 종료≥시작 refine, `approvers[]`. 수정 모드는 전 필드 optional(부분 수정) | F601·F602·F603, §참조 계약 매핑 | T1.1 | 종료<시작 시 폼 에러, 필수 누락 폼 에러, 수정 모드 partial 스키마 분기 | 7 | 4 | ☐ |
| T3.2 | 결재선 결재자 선택 UI: 사원 후보 조회 → `approvers[{approverId,role,order}]` 구성(순서 지정). **일반 사원 후보 조회 경로는 Open Q#1**(`DEPT_MEMBERS`/`DEPTS` 가정, 발명 금지 — 착수 전 확정) | §페이지별 상세(작성 주요기능), Open Q#1 | T3.1 | 결재자 검색·선택·순서 지정 → approvers[] 산출. 후보 조회 경로 미확정 시 //todo 플래그 후 사용자 질의 | 6 | 6 | ☐ |
| T3.3 | 작성/수정 API 함수 + mutation 훅: 임시저장(F601), 작성+상신(F602, 동일 body), 수정(F603, path draftId, 부분). onSuccess invalidate(내 휴가·상세) + 토스트 | F601·F602·F603, §참조 계약 매핑 | T3.1, T1.2 | 201(draftId 반환)·204 처리, 서버 규칙 위반(결재선 없음·잔여 초과 등)→에러 토스트(apiError 소비) | 7 | 5 | ☐ |
| T3.4 | 작성 페이지 조립(RHF+zod, 신규/수정 모드 분기) — **복잡도 7(폼 필드 5종 + 2개 mutation 버튼 + 신규/수정 모드 + 상세 프리필) → split** | §페이지별 상세(작성) | (하위 참조) | (하위 T3.4-a/b로 대체) | 7 | 7(split) | ☐ |
| T3.4-a | 작성(신규) 모드 조립: 제목/본문/유형/시작·종료 일시/결재선(T3.2) 필드 + [임시저장](F601)/[상신](F602) 버튼 → 성공 시 반환 `draftId`로 상세(M2) 이동 | §페이지별 상세(작성), F601·F602 | T3.2, T3.3 | 신규 작성으로 임시저장/상신 → 상세 이동 + 성공 토스트, 검증 실패 폼 에러 | 7 | 6 | ☐ |
| T3.4-b | 수정(edit) 모드: 상세(F606) 초기값 로드 → 폼 프리필 → F603 부분 수정 저장(204) → 상세 재조회 | §페이지별 상세(작성 수정 모드), F603 | T3.4-a, T2.2 | `draftId` 보유 진입 시 기존 값 프리필, 저장 성공 → 상세 재조회 + 토스트 | 6 | 5 | ☐ |
| T3.5 | 라우트 승격: `/leave/drafts/new`·`/leave/drafts/:draftId/edit`(가칭) ProtectedRoute 자식 추가 + 내 휴가 [휴가 신청](T1.5)·상세 [수정](M4에서 노출) 네비게이션 배선 | §메뉴 구조(하위 라우트) | T3.4-a, T3.4-b | [휴가 신청] → 작성 페이지, 상세 [수정] → 수정 모드 진입(EMPLOYEE 가드) | 4 | 3 | ☐ |

*(router.tsx 라우트·사이드바 비노출 하위 라우트 배선은 react-router-developer 위임 권장.)*

> **M3 split 판단(복잡도·중요도)**: T3.1(순수 zod 스키마 선언, API 없음, 작성 3기능+수정 partial 분기라 복잡도 4·후행 다수라 중요도 7)·T3.3(F601/F602/F603 3기능 mutation이나 표준 패턴 반복, 복잡도 5)은 임계값 미만. T3.2(결재자 후보 조회+선택+순서 지정 UI, **Open Q#1 미확정 리스크** 포함이라 복잡도 6)는 M3 최대 리스크. **T3.4는 폼 필드 5종 + 신규/수정 2모드 + 2개 mutation 버튼 + 상세 프리필이 한 태스크에 겹쳐 복잡도 7(임계값) → split**: T3.4-a(신규 모드 폼 조립 + 임시저장/상신 + 상세 이동, 복잡도 6·중요도 7)를 선행, T3.4-b(이미 만든 폼에 프리필/부분수정만 얹는 좁은 범위, 복잡도 5·중요도 6)를 후행으로 모드 축 분할. T3.5는 T1.6·T2.4와 동형 라우트 승격이라 복잡도 3.
> **실행 순서**: T3.1(중요도7) → T3.3(중요도7, T3.1·T1.2 의존) → T3.2(중요도6, T3.1 의존·T3.3과 병렬 가능) → T3.4-a(중요도7, T3.2·T3.3 의존) → T3.4-b(중요도6, T3.4-a·T2.2 의존) → T3.5(중요도4, M3 리프). 근거: 위상정렬 우선 + 동순위 내 중요도 높은 순. T3.2는 Open Q#1 미해결 시 병목이 될 수 있어 착수 즉시 후보 조회 경로를 사용자와 확정.

### M4 — 기안자 상세 액션 슬라이스 (F607 상신 · F608 철회 · F609 취소기안)

> 목표: 휴가 기안서 상세(M2)에서 **기안자 본인**이 상태에 따라 상신·철회·취소기안을 수행하는 슬라이스. 근거: PRD §페이지별 상세(휴가 기안서 상세 주요기능·다음 이동), F607·F608·F609.
> 완료 정의: 기안자+`UNSUBMITTED`→[상신](F607)/[수정](→M3), 기안자+`WAITING`/`IN_PROGRESS`→[상신 철회](F608), 기안자+`APPROVED`→[취소 기안 작성](F609). 성공(204/201)→상세·내 휴가 invalidate + 토스트, 규칙위반→서버 에러 토스트.

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T4.1 | 기안자 버튼 노출 판정 로직(순수 파생): 상세 응답으로 기안자 여부(`drafter.empId`==me)·상태별 노출(UNSUBMITTED→상신/수정, WAITING·IN_PROGRESS→철회, APPROVED+`cancellationDraftId==null`→취소기안). 최종은 서버 판정 — Open Q#4 | §페이지별 상세(버튼 노출 규칙), Open Q#4 | T2.1 | 4개 상태 × 기안자 판정 → 노출 버튼 집합 도출, 판정 기준 문서화 | 7 | 4 | ☐ |
| T4.2 | 기안자 단순 mutation 훅: 상신(F607, body optional approvers), 상신 철회(F608, body 없음). onSuccess invalidate(상세·내 휴가) + 토스트 | F607·F608, §참조 계약 매핑 | T2.2 | 204 처리, 차례 아님·기안자 아님 등 서버 위반→에러 토스트 | 7 | 4 | ☐ |
| T4.3 | 상세에 상신/철회 버튼 배선(T4.1 판정 × T4.2 mutation) | §페이지별 상세(휴가 기안서 상세) | T4.1, T4.2, T2.3 | 상태별 버튼 노출·클릭 동작, 성공 시 상세 즉시 갱신 | 6 | 4 | ☐ |
| T4.4 | 취소기안 mutation 훅 + 다이얼로그(F609 생성/생성+상신, `{title,content,approvers?}`, path `sourceDraftId`). 결재선 선택 UI(T3.2) 재사용 | F609, §참조 계약 매핑 | T4.1, T2.2 | 기안자+APPROVED 행에서만 오픈, 201(draftId) → 상세 갱신/이동 + 토스트, 검증 실패 폼 에러 | 5 | 6 | ☐ |

> **M4 split 판단(복잡도·중요도)**: 전 태스크 복잡도 < 7 → **split 없음(4개 전부 단일 task 유지)**. T4.1(캐시된 상세 데이터 파생 순수 로직, API 없음, Open Q#4 판정 기준이라 복잡도 4·후행 다수 중요도 7)·T4.2(F607+F608 body-less/단순 204 mutation 2종, 복잡도 4)·T4.3(신규 API 없이 T4.1×T4.2 배선, 복잡도 4)은 낮음. T4.4(F609는 2개 엔드포인트 묶음 + `{title,content,approvers}` 미니폼 다이얼로그, T3.2 결재선 UI 재사용이나 취소기안 생성/상신 분기까지라 복잡도 6)를 단순 상신/철회와 분리해 M4 최대 복잡도로 격리.
> **실행 순서**: T4.1(중요도7) → T4.2(중요도7, T2.2 의존·T4.1과 병렬 가능) → T4.3(중요도6, T4.1·T4.2·T2.3 의존) → T4.4(중요도5, T4.1·T2.2 의존, M4 리프). 근거: 위상정렬 우선 + 동순위 내 중요도 높은 순. T4.1/T4.2가 마일스톤 핵심(상신·철회)을 직접 실현하는 선행조건이라 T4.4보다 높은 중요도.

### M5 — 결재대기함 + 결재자 액션 슬라이스 (F619 목록 · F610 승인 · F611 반려)

> 목표: 사이드바 "결재대기함"(EMPLOYEE 공통) → 내 결재 차례 문서 목록 → 항목 클릭 → 상세(M2) → [승인]/[반려]. 근거: PRD §사용자 여정(결재대기함), §페이지별 상세(결재대기함 페이지), F619·F610·F611.
> 완료 정의: 결재자가 role·부서 무관하게 대기함으로 진입해 상세에서 승인(F610)/반려(F611, 사유 필수)를 수행. 성공(204)→상세·대기함 invalidate + 토스트. **이 슬라이스가 결재선 결재자(일반 사원·타 부서 포함)의 상세 진입 유일 경로를 완성한다(PRD #8 해결의 실현).** 목록에 혼재하는 휴가 외 기안 유형은 상세 진입 후 `draftType` 분기로 폴백(Open Q#7).
> **왜 하나의 슬라이스인가**: PRD가 명시하듯 "결재대기함의 존재 이유가 승인/반려 액션 접근"이므로, 대기함 조회와 결재자 액션을 분리하면 어느 쪽도 완결된 슬라이스가 못 된다 → 하나의 세로 슬라이스로 묶는다.

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T5.1 | 결재대기함 타입(`PendingApprovalDraftRow`) + `leaveKeys` 축 확장(`pendingApprovals(params)`) | §참조 계약 매핑(`MY_PENDING_APPROVAL_DRAFTS`) | T1.1, T1.2 | 타입·키 존재, 스니펫 대조 통과 | 7 | 3 | ☐ |
| T5.2 | 결재대기함 API 함수 + query 훅: F619(`keyword`/page/size, **페이징 `number+1`**) | F619, §참조 계약 매핑 | T5.1 | 페이징 메타(number+1) 반영, 조회 실패 에러 토스트 | 7 | 4 | ☐ |
| T5.3 | 결재대기함 페이지 조립: 제목 키워드 검색·페이징 표(react-table), `isFileAttached` 첨부 아이콘 표시만, 행 클릭→상세(M2). 혼재 유형 식별(제목/기안자) — Open Q#7 | §페이지별 상세(결재대기함), F619, Open Q#7 | T5.2, T1.3 | 검색·페이징·행클릭 동작, 첨부 아이콘 렌더 | 6 | 5 | ☐ |
| T5.4 | 결재자 버튼 노출 판정 로직(순수 파생): 상세 응답 `approvers[]`로 "현재 내 결재 차례" 도출(본인이 결재선에 있고 이전 order 전부 처리·본인 미처리). 최종은 서버 — Open Q#4 | §페이지별 상세(버튼 노출 규칙), Open Q#4 | T2.1 | 승인/반려 노출 여부 도출, 차례 아님·이미 처리 케이스 판정 문서화 | 7 | 5 | ☐ |
| T5.5 | 결재자 mutation 훅: 승인(F610, body 없음), 반려(F611, `reason` 필수·공백 불가). onSuccess invalidate(상세·대기함) + 토스트 | F610·F611, §참조 계약 매핑 | T2.2 | 204 처리, 차례 아님·이미 처리·결재선 밖→서버 에러 토스트(apiError 소비) | 7 | 4 | ☐ |
| T5.6 | 상세에 승인/반려 버튼 배선(T5.4 판정 × T5.5 mutation) + 반려 사유 다이얼로그(RHF+zod, reason 공백 불가) | §페이지별 상세(휴가 기안서 상세) | T5.4, T5.5, T2.3 | 결재 차례 시 버튼 노출·클릭, 반려 사유 미입력 폼 에러, 성공 시 상세·대기함 갱신 | 6 | 5 | ☐ |
| T5.7 | 라우트 승격: `/leave/pending-approvals`(가칭) ProtectedRoute(EMPLOYEE) 자식 추가 + 사이드바 "휴가 관리" 그룹에 **"결재대기함" 신규 슬롯 추가**(minRole EMPLOYEE) + 상세 진입 시 `draftType`≠휴가 폴백 안내(Open Q#7) | §메뉴 구조, Open Q#7 | T5.3, T5.6 | 대기함 메뉴 진입(EMPLOYEE), 행클릭→상세→승인/반려 관통, 비휴가 유형 폴백 안내 | 4 | 3 | ☐ |

*(사이드바 신규 슬롯·router.tsx 배선은 react-router-developer 위임 권장. "결재대기함"은 임시로 휴가 그룹에 두며 추후 전자결재 도메인 PRD 착수 시 전자결재 그룹으로 이관 가능 — PRD §메뉴 구조 주석.)*

> **M5 split 판단(복잡도·중요도)**: 전 태스크 복잡도 < 7 → **split 없음(7개 전부 단일 task 유지)**. T5.1(타입+키 확장, 복잡도 3)·T5.2(F619 단일 GET+페이징, 복잡도 4)·T5.5(F610/F611 mutation 2종, 복잡도 4)는 표준 패턴. T5.3(검색+페이징 표+첨부 아이콘+혼재 유형 식별, 복잡도 5)·T5.4(결재선 order 순회로 "현재 내 차례" 도출하는 순수 파생, 기안자 판정보다 로직이 복잡해 5)·T5.6(승인/반려 배선+반려 사유 다이얼로그, 복잡도 5)는 중간값. 7개 태스크로 마일스톤이 크지만 각 태스크는 임계값 미만이며, 대기함 조회 3종(T5.1~T5.3)과 결재자 액션 4종(T5.4~T5.7)이 "결재자 진입→승인/반려" 하나의 세로 슬라이스를 이룬다.
> **실행 순서**: T5.1(중요도7) → T5.2(중요도7, T5.1 의존) → T5.4(중요도7, T2.1 의존·대기함 조회와 병렬 가능) → T5.5(중요도7, T2.2 의존) → T5.3(중요도6, T5.2·T1.3 의존) → T5.6(중요도6, T5.4·T5.5·T2.3 의존) → T5.7(중요도4, T5.3·T5.6 의존, M5 리프). 근거: 위상정렬 우선 + 동순위 내 중요도 높은 순. 대기함 조회축(T5.1→T5.2→T5.3)과 결재자 액션축(T5.4→T5.5→T5.6)은 상호 독립이라 병렬 진행 가능하며, T5.7이 둘을 라우트로 합류시킨다.

### M6 — 부서 휴가 관리 조회 슬라이스 (F612 이력 · F613 부서원 요약 · F614 부서 사용률)

> 목표: `DEPT_MANAGER` 이상만 진입하는 부서 휴가 관리 페이지에서 탭①신청 이력·탭②부서원 잔여/사용률을 조회하는 슬라이스. 근거: PRD §사용자 여정(부서 휴가 관리), §페이지별 상세(부서 휴가 관리 페이지), F612·F613·F614.
> 완료 정의: 메뉴 게이팅(EMPLOYEE 미노출)·탭 전환·부서원 keyword/상태/월·연도 필터·페이징 동작, 본인 부서(`deptId`) 축으로 조회, 타 부서 접근 시 403(`ROLE_003`) 권한부족 UX. 이력 행 클릭 → 상세(M2, 부서 신청 현황 열람용 — 승인/반려는 결재대기함이 표준 경로).

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T6.1 | 부서 타입: `DeptLeaveHistoryRow`(사원정보+historyResponse 단건)·`EmpLeaveSummaryRow`(사원+leaveSummary, **`empId` 포함**=Open Q#5 해결). 사용률은 단일값(`annualLeaveUsagePercent`) | §참조 계약 매핑(`DEPT_LEAVE_REQUEST_HISTORY`/`DEPT_EMP_LEAVE_SUMMARY`/`DEPT_EMP_LEAVE_USAGE_SUMMARY`) | T1.1 | 3개 응답 타입 정의, 스니펫 대조(empId 포함 확인) | 7 | 3 | ☐ |
| T6.2 | 본인 부서 `deptId` 도출: 기존 `usePrimaryDeptId`(attendance) 재사용(필요 시 `src/shared` 승격). 복수부서 선택 UI 필요성은 Open Q#2 | §권한 분기점, Open Q#2 | — | deptId 확정 전 조회 훅 `enabled:false` 대기, 재사용 확인 | 6 | 2 | ☐ |
| T6.3 | `leaveKeys` 부서 축 추가 + API·query 훅 3종: F612(이력 keyword/status/yearMonth/page/size), F613(요약 keyword/year/page/size), F614(사용률 year, 단일값) | F612·F613·F614, §참조 계약 매핑 | T6.1, T1.2 | 페이징 메타(number+1) 반영(F612/F613), F614 단일값, 403→권한부족 매핑 소비 | 8 | 6 | ☐ |
| T6.4 | 부서 휴가 관리 페이지 조립(shadcn Tabs) — **복잡도 7(2개 탭 × 상이한 데이터 + 탭별 독립 필터·페이징) → split** | §페이지별 상세(부서 휴가 관리) | (하위 참조) | (하위 T6.4-a/b로 대체) | 7 | 7(split) | ☐ |
| T6.4-a | Tabs 셸 + 탭① 신청 이력 조립(부서원 검색/상태/월 필터 + 표 + 페이징 + 행클릭→상세) | §페이지별 상세(부서 휴가 관리), F612 | T6.2, T6.3, T1.3, T2.4 | 필터 변경 시 재조회, 표·페이징 렌더, 행클릭→상세, 탭② 슬롯 보유 | 7 | 6 | ☐ |
| T6.4-b | 탭② 부서원 잔여/사용률 조립(사용률 카드 F614 + 부서원 요약 표 F613 + year 필터 + 페이징) + 탭①↔② 상태 독립 유지 | §페이지별 상세(부서 휴가 관리), F613·F614 | T6.4-a | 사용률 카드+요약 표+페이징 렌더, 탭 전환해도 각 탭 상태 보존 | 6 | 6 | ☐ |
| T6.5 | 라우트 승격: `/leave/dept`(가칭) ProtectedRoute+`DEPT_MANAGER` 게이팅 + 사이드바 "부서 휴가 관리" placeholder→승격(minRole DEPT_MANAGER) | §메뉴 구조, §권한 분기점 | T6.4-b | `EMPLOYEE` 미노출, `ADMIN` 계층 자동 노출, `DEPT_MANAGER` 진입 가능 | 4 | 3 | ☐ |

*(router.tsx role 가드·사이드바 게이팅 배선은 react-router-developer 위임 권장.)*

> **M6 split 판단(복잡도·중요도)**: T6.1(3개 응답 순수 타입선언, empId 보강 반영, 복잡도 3·후행 재사용 중요도 7)·T6.2(캐시된 me에서 부서 도출, 기존 훅 재사용이라 복잡도 2가 최저)·T6.3(F612+F613+F614 3기능 + path deptId + 403 매핑, 근태 T3.3와 동급 패턴이라 복잡도 6·F612~F614 직접 실현 중요도 8)은 임계값 미만. **T6.4는 2개 탭이 상이한 데이터(신청이력 vs 잔여/사용률)를 각각 별도 필터·페이징으로 렌더 → 근태 T3.4와 동형 복잡도 7 → split**: T6.4-a(Tabs 셸+탭① 이력, 검색/상태/월 3종 필터+행클릭 상세, 복잡도 6·중요도 7)를 선행, T6.4-b(이미 만든 셸에 탭② 사용률 카드+요약 표만 추가, 복잡도 6·중요도 6)를 후행으로 탭 축 분할. T6.5는 근태 T3.5와 동형 게이팅 승격이라 복잡도 3.
> **실행 순서**: T6.1(중요도7) → T6.2(중요도6, T6.1과 독립·병렬 가능하나 동순위 내 중요도 낮아 후순) → T6.3(중요도8, T6.1 의존) → T6.4-a(중요도7, T6.2·T6.3·T2.4 의존) → T6.4-b(중요도6, T6.4-a 의존) → T6.5(중요도4, M6 리프). 근거: 위상정렬 우선 + 동순위 내 중요도 높은 순. T6.1·T6.2는 상호 독립이라 병렬 착수 가능.

### M7 — 관리자 휴가 현황 슬라이스 (F615·F616 조회 + F617·F618 부여일수 조정)

> 목표: `ADMIN`만 진입해 전사/부서 연차 사용률·전사 사원 휴가 요약을 조회하고, 특정 사원의 특별/포상 부여일수를 조정하는 슬라이스. 근거: PRD §사용자 여정(관리자 휴가 현황), §페이지별 상세(관리자 휴가 현황 페이지), F615·F616·F617·F618.
> 완료 정의: 이름/부서/연도 필터·페이징으로 요약·사용률 조회. 요약 행 [특별/포상 조정] → `plusMinusDays`(음수 허용) 입력·제출(204) → 요약·사용률 invalidate + 토스트. 조정 대상 `empId`는 요약 응답 행에서 직접 사용(Open Q#5 해결).

| Task | 설명 | 근거(PRD) | Depends-on | Done 조건 | 중요도 | 복잡도 | 완료 여부 |
|---|---|---|---|---|---|---|---|
| T7.1 | 관리자 타입: `EmpLeaveSummaryRow`(M6 T6.1과 동형, `empId` 포함) 재사용/공유 + 부여일수 조정 요청 타입(`plusMinusDays`) | §참조 계약 매핑(`EMP_LEAVE_SUMMARY`/`EMP_LEAVE_ADJUST_*`) | T1.1 | 요약 행 타입 재사용, 조정 파라미터 타입 정의 | 6 | 2 | ☐ |
| T7.2 | `leaveKeys` 전사 축 추가 + 조회 API·query 훅: F615(전사 요약 keyword/deptId/year/page/size), F616(사용률 deptId/year, 단일값). 부서 필터 후보는 `DEPTS` 조회(Open Q — 부서 목록 출처 확정) | F615·F616, §참조 계약 매핑 | T7.1, T1.2 | 페이징 메타(number+1) 반영(F615), F616 단일값, ADMIN 게이트 | 7 | 5 | ☐ |
| T7.3 | 부여일수 조정 mutation 훅: 특별(F617, path empId + query plusMinusDays), 포상(F618, 동형). onSuccess invalidate(요약·사용률) + 토스트 | F617·F618, §참조 계약 매핑 | T7.1, T1.2 | 204 처리, 음수 초과 등 서버 규칙 위반→에러 토스트 | 7 | 4 | ☐ |
| T7.4 | 관리자 페이지 조립: 사용률 카드(F616) + 전사 요약 표(F615, 이름 검색/부서/연도 필터 + 페이징) + 부서 필터 셀렉트 | §페이지별 상세(관리자 휴가 현황), F615·F616 | T7.2, T1.3 | 사용률·요약 표·필터·페이징 렌더, 조회 실패 에러 토스트 | 6 | 6 | ☐ |
| T7.5 | 부여일수 조정 다이얼로그(shadcn Dialog + RHF+zod): 요약 행 [특별/포상 조정] → `plusMinusDays` 정수/부호 검증 → 제출(T7.3). `empId`는 행 응답값 사용 | §페이지별 상세(관리자 휴가 현황), F617·F618, Open Q#5(해결) | T7.3, T7.4 | 행에서 다이얼로그 오픈, 검증 실패 폼 에러, 성공→요약·사용률 갱신 + 토스트 | 6 | 5 | ☐ |
| T7.6 | 라우트 승격: `/leave/admin`(가칭) ProtectedRoute+`ADMIN` 게이팅 + 사이드바 "휴가 관리" 그룹에 **"관리자 휴가 현황" 신규 슬롯 추가**(minRole ADMIN) | §메뉴 구조, §권한 분기점 | T7.5 | `EMPLOYEE`·`DEPT_MANAGER` 미노출, `ADMIN` 진입 가능 | 4 | 3 | ☐ |

*(router.tsx role 가드·사이드바 신규 슬롯 배선은 react-router-developer 위임 권장.)*

> **M7 split 판단(복잡도·중요도)**: 전 태스크 복잡도 < 7 → **split 없음(6개 전부 단일 task 유지)**. T7.1(M6 요약 타입 재사용+조정 파라미터, 복잡도 2가 최저)·T7.3(F617/F618 query-param mutation 2종, path empId 요약행값 그대로, 복잡도 4)은 낮음. T7.2(F615+F616 2기능 GET + 부서 필터 목록, 복잡도 5)·T7.4(사용률 카드+요약 표+3종 필터+부서 셀렉트, 근태 T1.5와 동급 6)·T7.5(조정 다이얼로그+RHF/zod 부호검증, 근태 T4.3 다이얼로그와 동급 5)는 중간값. F615 요약 표와 F617/F618 조정이 한 페이지에서 요약행→다이얼로그로 직결(empId 보강 덕분)되어, 표 조립(T7.4)과 다이얼로그(T7.5)를 분리하되 같은 마일스톤에 둔다.
> **실행 순서**: T7.1(중요도6) → T7.2(중요도7, T7.1 의존) → T7.3(중요도7, T7.1 의존·T7.2와 병렬 가능) → T7.4(중요도6, T7.2 의존) → T7.5(중요도6, T7.3·T7.4 의존) → T7.6(중요도4, M7 리프). 근거: 위상정렬 우선 + 동순위 내 중요도 높은 순. T7.2·T7.3은 상호 독립(조회 vs 조정)이라 병렬 가능.

## 🔀 병렬화 가능 지점

- **T1.1(타입)과 T1.2(queryKeys)**: 상호 독립 → 병렬. T1.3(표시 유틸)도 T1.1 이후 T1.4와 병렬 가능.
- **M4(기안자 액션)와 M5(결재자 액션)**: 둘 다 M2(상세 페이지 셸)에만 의존하고 서로 독립 → 두 슬라이스 병렬 착수 가능. M5 내부에서도 대기함 조회축(T5.1→T5.2→T5.3)과 결재자 액션축(T5.4→T5.5→T5.6)이 독립이라 병렬 진행 가능.
- **M6(부서 조회)·M7(관리자)**: 둘 다 M1에만 의존(조회·요약 부분)하고 서로 독립 → 병렬 착수 가능. 나아가 M2~M5 클러스터와도 병렬 가능하되, "이력 행 클릭 → 상세" 네비게이션 배선(T6.4-a의 T2.4 의존)만 M2 완료를 기다린다. M7은 상세 진입이 없어 M2와 완전 독립.
- **T3.1(스키마)·T3.3(mutation)**과 **T3.2(결재선 UI)**: T3.1 이후 부분 병렬 가능.
- **T4.1(기안자 판정)·T4.2(mutation)**, **T7.2(조회)·T7.3(조정)**: 각 쌍 상호 독립 → 병렬.
- **T6.1(타입)·T6.2(deptId 도출)**: 독립 → 병렬.

## ⚠️ 리스크 & 선행 결정 (Open Questions — PRD §Open Questions 승계)

- **라우트 경로 명명(신규 결정)**: 휴가 페이지들의 실제 경로(`/leave/me`·`/leave/drafts/:draftId`·`/leave/drafts/new`·`/leave/drafts/:draftId/edit`·`/leave/pending-approvals`·`/leave/dept`·`/leave/admin`는 전부 가칭)를 기존 router 컨벤션(board/attendance)에 맞춰 착수 전 확정. 휴가 그룹 슬롯은 PRD §메뉴 구조를 따른다.
- **[Open Q#1] 결재선 결재자 후보 검색 경로(M3 T3.2 병목 리스크)**: 일반 사원이 `approvers[].approverId`를 지정할 표준 사원 검색 경로 미확정. `EMPS_FOR_MANAGEMENT`/`NEW_EMP_LIST`는 HR/DEPT_MANAGER 전용이라 불가, `DEPT_MEMBERS`/`DEPTS`가 유일 후보로 보임 → **T3.2 착수 즉시 사용자와 확정**(발명 금지, 미확정 시 //todo 플래그). M4 T4.4(취소기안 결재선)도 동일 UI 재사용하므로 영향.
- **[Open Q#2] 부서 deptId 출처(M6 T6.2)**: 기존 `usePrimaryDeptId`(isPrimary 부서)를 조회 축으로 가정. 매니저가 복수 부서를 관리하는 경우 부서 선택 UI 필요성 확인 — 필요 시 M6에 태스크 추가.
- **[Open Q#3] `HOURLY` 휴가 유형 라벨(M1 T1.3)**: enum description "공휴일"과 "연가 1시간 단위 사용" 규칙이 충돌 → 화면 라벨과 `reservedHours`("n시간") 표기 규칙을 T1.3 착수 전 확정.
- **[Open Q#4] 상세 액션 버튼 노출 판정(M4 T4.1 · M5 T5.4)**: "현재 내 결재 차례"·기안자 상태별 노출·취소기안 노출(원본 APPROVED + `cancellationDraftId==null`)을 상세 응답 필드로 도출하는 규칙을 확정. 프론트는 노출만 담당하고 최종 판단은 서버(403/도메인 에러 폴백).
- **[Open Q#6] 첨부파일 범위(M2 T2.3)**: 상세 `files[]`는 **표시만**(업로드/뷰어 제외). 업로드/미리보기/다운로드를 이번 범위에 넣을지, `@docs/backend-contract/file-upload.md` 위임할지 확정 — 현재는 표시만 가정.
- **[Open Q#7] 결재대기함 혼재 유형 폴백(M5 T5.3 · T5.7)**: `MY_PENDING_APPROVAL_DRAFTS`는 휴가 외 기안(일반/출장/매출)도 섞여 오고 목록에 `draftType`이 없다. 이번 범위는 상세 진입 후 `DRAFT_DETAIL.draftType`으로 분기해 **휴가만 렌더**, 그 외는 "전자결재 도메인에서 처리 예정" 폴백 안내로 가정. 목록 단계 유형 필터/뱃지·폴백 UX(비활성/안내) 확정 필요.
- **[신규 확인] 관리자 부서 필터 목록 출처(M7 T7.2)**: F615/F616 `deptId` 필터 셀렉트 후보를 `DEPTS`(조직도 도메인 기존 조회) 재사용으로 가정 — 기존 훅 존재 여부 확인 후 재사용.
- **[해결됨] Open Q#5**: 부서/전사 요약 응답 `content[]`에 `empId` 백엔드 보강 완료 → F615/F613 요약 행에서 받은 `empId`를 F617/F618 path에 그대로 사용(T7.5/T6.1 전제). 별도 조정 대상 검색 불필요.
- **[해결됨] Open Q#8**: 결재대기함(F619) 포함으로 결재선 결재자(일반 사원·타 부서 포함)의 상세 진입 유일 경로 확보 → M5가 이를 실현. 상세는 신규 생성 없이 M2(F606) 재사용.

## 📦 백로그 (PRD 명시 제외 — 태스크화 금지)

PRD §"MVP 이후 기능(이번 PRD 명시적 제외)" 참조로만 나열(향후 별도 PRD 대상):

- **전자결재 공용 문서함 3종 + 부가 API**: 결재함(`MY_ACCESSIBLE_DOCUMENTS`)·기안함(`MY_SUBMITTED_DRAFTS`)·임시저장함(`MY_UNSUBMITTED_DRAFTS`)·`MY_PENDING_APPROVAL_DRAFTS_COUNT`·문서함 요약(`MY_DOCUMENT_BOX_SUMMARY`) — 전자결재 도메인 PRD 관할. **결재대기함(`MY_PENDING_APPROVAL_DRAFTS`)만 F619로 최소 포함**(추후 전자결재 그룹으로 이관 가능).
- **일반/출장/매출 기안 작성**(`GENERAL_DRAFT_*`/`BUSINESS_TRIP_DRAFT_*`/`SALES_DRAFT_*`)·출장 참여자 수정 — 별도 도메인. 결재대기함 혼재 유형은 폴백 안내로만 처리(Open Q#7).
- **공람자 추가/제거/읽음**(`DRAFT_CIRCULATION_*`) — 전자결재 도메인. 상세 `circulations[]`는 표시조차 이번 범위 밖.
- **기안서 첨부파일 업로드/미리보기/다운로드/삭제**(`DRAFT_FILE_*`) — 파일 도메인. 상세 `files[]` 표시만 이번 범위.
- **휴가↔근태/일정 연동**(승인 시 근태·Schedule 반영) — 백엔드 이벤트 처리, 프론트 화면 없음.
- **연차 자동 부여(1/1 배치)·잔여 초과 사전 차단** — 서버 정책/판정(프론트는 잔여 표시만, Open Q#2).
- 잔여/사용률 시각화(Recharts), 테마/다크모드, i18n, 브라우저 푸시 알림.

## ✅ 정합성 검증 체크리스트 (실행 결과)

- 🔍 **커버리지**: F601(T3.3/T3.4-a)·F602(T3.3/T3.4-a)·F603(T3.1/T3.3/T3.4-b)·F604(T1.4/T1.5)·F605(T1.4/T1.5)·F606(T2.2/T2.3)·F607(T4.2/T4.3)·F608(T4.2/T4.3)·F609(T4.4)·F610(T5.5/T5.6)·F611(T5.5/T5.6)·F612(T6.3/T6.4-a)·F613(T6.3/T6.4-b)·F614(T6.3/T6.4-b)·F615(T7.2/T7.4)·F616(T7.2/T7.4)·F617(T7.3/T7.5)·F618(T7.3/T7.5)·F619(T5.2/T5.3) — 19개 전부 ≥1 태스크 매핑 ✅
- 🔍 **역참조**: 모든 태스크가 PRD F60x/§페이지별 상세/§참조 계약 매핑/§Open Questions에 근거 — 발명 태스크 없음 ✅
- 🔍 **의존성**: M1→M2→(M3·M4·M5), M1→(M6·M7) 위상 정렬, 순환 없음. 기존 배관(M0·인증·셸·근태 usePrimaryDeptId)은 재구축하지 않고 소비 전제 ✅
- 🔍 **여정 정합**: 태스크 순서가 PRD 사용자 여정(내 휴가 조회→상세 열람→작성/수정→기안자 액션→결재대기함/결재자 액션→부서→관리자)과 일치 ✅
- 🔍 **범위**: PRD 제외 기능(문서함 3종·공람·타 기안 작성·첨부 업로드·연동/배치)은 백로그로만, 태스크화 없음. 결재대기함만 F619로 최소 포함(근거 명시) ✅
- 🔍 **규약**: 계약/전역 규칙(reissue·페이징+1·ROLE_003·withCredentials·날짜 dayjs) 재서술 없음, 필드/DTO 설계 없음(스니펫·§참조 계약 매핑으로 위임), URL/인프라/견적 강제 없음(경로는 가칭 + react-router-developer 위임) ✅

**결과: 6개 항목 전부 통과. 기존 배관 소비 전제로 휴가 도메인 M1~M7만 설계 완료. Blocking 결함 2건(요약→조정 empId #5 / 결재자 상세 진입 #8)은 PRD 단계에서 이미 해결. 잔여 Open Questions(#1·#2·#3·#4·#6·#7)는 비블로킹으로 격리 — F601~F619 착수 가능.**
