{
  "info": {
    "name": "Franchise Fake API",
    "description": "Postman collection for the Mockoon franchise fake API dataset from 2025-01-01 to 2026-12-31.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/health",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "health"
          ]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status is 200', function () { pm.response.to.have.status(200); });",
              "pm.test('service is up', function () { const body = pm.response.json(); pm.expect(body.status).to.eql('UP'); });"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Daily Sales",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/daily-sales",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "api",
            "daily-sales"
          ]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status is 200', function () { pm.response.to.have.status(200); });",
              "pm.test('syncType is DAILY_SALES', function () { const body = pm.response.json(); pm.expect(body.syncType).to.eql('DAILY_SALES'); pm.expect(body.items).to.be.an('array'); });"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Inquiries",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/inquiries",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "api",
            "inquiries"
          ]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status is 200', function () { pm.response.to.have.status(200); });",
              "pm.test('syncType is INQUIRY', function () { const body = pm.response.json(); pm.expect(body.syncType).to.eql('INQUIRY'); pm.expect(body.items).to.be.an('array'); });"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Education Applications",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/education-applications",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "api",
            "education-applications"
          ]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status is 200', function () { pm.response.to.have.status(200); });",
              "pm.test('syncType is EDUCATION_APPLICATION', function () { const body = pm.response.json(); pm.expect(body.syncType).to.eql('EDUCATION_APPLICATION'); pm.expect(body.items).to.be.an('array'); });"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Education Application Cancellations",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/education-application-cancellations",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "api",
            "education-application-cancellations"
          ]
        }
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status is 200', function () { pm.response.to.have.status(200); });",
              "pm.test('syncType is EDUCATION_APPLICATION_CANCEL', function () { const body = pm.response.json(); pm.expect(body.syncType).to.eql('EDUCATION_APPLICATION_CANCEL'); pm.expect(body.items).to.be.an('array'); });"
            ],
            "type": "text/javascript"
          }
        }
      ]
    }
  ]
}