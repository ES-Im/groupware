package com.haruon.groupware.domain.empInfo.attendance;

import static org.junit.jupiter.api.Assertions.*;

// to-do 스케쥴 도메인 완성 후 해야할 테스트
class AttendanceCloseParamTest {

    // 근태 기준 날짜는 null일 수 없음 & 사원정보가 없음
    
    // empAttendance가 널이 아니면 출근기록과 근태날짜 있어야함
    
    // schedule은 null이 안됌 + 스케쥴 요소는 null이 안됌
    
    // 하나의 param에 종일 일정은 최대 1개이며 다른 일정은 못들어감
    
    // 연차는 하루에 1개만 허용됨 (연차 + 연차 안되고 연차 + 출장은 됌)
    
    // 스케쥴에 수기등록/미팅은 안들어감
    
    // 일정에는 시작시각, 종료시각이 있어야 하고 일정 종료시각은 시작시각보다 빠르면 안됨

}